apiVersion: v1
kind: Service
metadata:
  name: {{ include "sympauthy.fullname" . }}
  labels:
    {{- include "sympauthy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sympauthy.selectorLabels" . | nindent 4 }}
